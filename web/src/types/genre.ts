/**
 * Genre types.
 *
 * Prisma Genre model (import { Genre } from '@prisma/client').
 */

import type { Genre as PrismaGenre } from '@prisma/client';

/** Re-export Prisma Genre type for convenience. */
export type Genre = PrismaGenre;
