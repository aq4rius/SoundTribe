// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Schema models will be added in Phase 1 when we begin the database migration.
// They will mirror the Mongoose models in server/src/models/ with proper
// PostgreSQL types and relations.
